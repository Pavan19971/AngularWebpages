import { BusFilterPipe } from './bus-filter.pipe';

describe('BusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
