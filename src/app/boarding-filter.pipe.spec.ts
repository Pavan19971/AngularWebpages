import { BoardingFilterPipe } from './boarding-filter.pipe';

describe('BoardingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
